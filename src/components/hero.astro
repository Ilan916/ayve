<div class="bg-white">
  <script type="module" src="/scripts/appear.js"></script>
  <div class="mx-auto max-w-7xl py-12 sm:px-6 sm:py-14 lg:px-8">
    <div class="relative isolate overflow-hidden bg-[#1e274a] px-6 py-24 text-center shadow-2xl sm:rounded-3xl sm:px-16">
      <h2 class="text-balance text-5xl font-semibold tracking-tight text-white sm:text-5xl font-general-sans animate-on-scroll">Un site moderne qui attire des clients et inspire confiance</h2>
      <p class="mx-auto font-semibold mt-6 max-w-3xl text-pretty text-lg/8 text-gray-300 font-satoshi animate-on-scroll">
        75% des internautes jugent la crédibilité d’une entreprise à la qualité de son site web.  
      </p>
      <p class="mx-auto max-w-3xl text-pretty text-lg/8 text-gray-300 font-satoshi animate-on-scroll"> 
        Faites en sorte que la vôtre vous apporte des clients dès la première impression.
      </p>
      <div class="mt-10 flex items-center justify-center gap-x-6 animate-on-scroll">
  <a href="#contact-form" class="rounded-md bg-[#5974be] px-3.5 py-2.5 text-sm font-semibold text-[#fff] shadow-sm transform transition duration-200 hover:shadow-lg hover:-translate-y-1 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white font-satoshi scroll-to-contact"> Obtenir mon audit gratuit </a>
          <p class="text-sm/6 font-semibold text-white font-satoshi italic">
          Sans engagement
        </p>
</div>

      </div>
      <svg viewBox="0 0 1024 1024" aria-hidden="true" class="absolute left-1/2 top-1/2 -z-10 size-[64rem] -translate-x-1/2 [mask-image:radial-gradient(closest-side,white,transparent)] animate-on-scroll">
        <circle r="512" cx="512" cy="512" fill="url(#827591b1-ce8c-4110-b064-7cb85a0b1217)" fill-opacity="0.7" />
        <defs>
          <radialGradient id="827591b1-ce8c-4110-b064-7cb85a0b1217">
            <stop stop-color="#5974be" />
            <stop offset="1" stop-color="#5974be" />
          </radialGradient>
        </defs>
      </svg>
    </div>
  <dl class="mt-16 grid grid-cols-1 gap-0.5 overflow-hidden rounded-2xl text-center sm:grid-cols-2 lg:grid-cols-4">
      <div class="flex flex-col bg-gray-400/5 p-8">
  <dt class="text-sm/6 font-general-sans font-semibold text-gray-600 animate-on-scroll">Livraison rapide et garantie</dt>
  <dd class="order-first text-3xl font-satoshi font-semibold tracking-tight text-gray-900 animate-on-scroll">21 jours</dd>
      </div>
      <div class="flex flex-col bg-gray-400/5 p-8">
  <dt class="text-sm/6 font-general-sans font-semibold text-gray-600 animate-on-scroll">Sites optimisés pour mobile et SEO</dt>
  <dd class="order-first text-3xl font-satoshi font-semibold tracking-tight text-gray-900 animate-on-scroll">100%</dd>
      </div>
      <div class="flex flex-col bg-gray-400/5 p-8">
  <dt class="text-sm/6 font-general-sans font-semibold text-gray-600 animate-on-scroll">Expérience en création de sites</dt>
  <dd class="order-first text-3xl font-satoshi font-semibold tracking-tight text-gray-900 animate-on-scroll">+4 ans</dd>
      </div>
      <div class="flex flex-col bg-gray-400/5 p-8">
  <dt class="text-sm/6 font-general-sans font-semibold text-gray-600 animate-on-scroll">Prix clairs, sans frais cachés</dt>
  <dd class="order-first text-3xl font-satoshi font-semibold tracking-tight text-gray-900 animate-on-scroll">0% surprise</dd>
      </div>
    </dl>
  </div>
</div>


<script>
  document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('a.scroll-to-contact').forEach(function(link) {
      link.addEventListener('click', function(e) {
        const target = document.getElementById('contact-form');
        if (target) {
          e.preventDefault();
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  });
</script>